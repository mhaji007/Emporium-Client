import React from "react";
import Layout from '../core/Layout';
import {API} from "../config";

const Signup = () => {
  return <Layout title="Signup" description="Sign up">
    {API}
  </Layout>;
};

export default Signup;
